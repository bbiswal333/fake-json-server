{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "json fake server",
  "dependencies": {
    "json-server": "latest"
  },
  "scripts": {
    "start": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
